<h1> <%= @band.name %> </h1>


<p>Choose which venues <%= @band.name %> has jammed upon:</p>

<% if @venues.any? %>

  <form action="/bands/<%= @band.id %>" method="post">
    <% @venues.each do |venue| %>
        <label>
          <input type="checkbox" name="venue_ids[]" value="<%= venue.id %>"><%= venue.name %>
        </label>
    <% end %>
    <button type="submit">Add venues</button>
  </form>

<% end %>

<% if @band.venues.any? %>

  <h3>Here are the venues which <%= @band.name %> has played at:</h3>

  <ul>
    <% @band.venues.each do |venue| %>
      <li><%= venue.name %></li>
    <% end %>
  </ul>
<% else %>
  <p>They haven't played anywhere yet!</p>
<% end %>


<a href="/">Go back</a>
